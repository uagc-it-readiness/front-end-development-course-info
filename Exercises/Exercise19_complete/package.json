{
  "name": "gulp-starter",
  "version": "1.0.0",
  "description": "In the folder labeled 'basic' you will find an HTML document with content that should display in a modal.  Write code to fulfill the following requirements.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch server/db.json -d 1000 --port 3005"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.7",
    "gulp": "^3.9.1",
    "gulp-json-srv": "^1.1.0"
  }
}
